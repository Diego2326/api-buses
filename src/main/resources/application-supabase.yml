spring:
  datasource:
    url: ${SUPABASE_DB_URL:jdbc:postgresql://aws-1-us-west-2.pooler.supabase.com:5432/postgres?sslmode=require}
    username: ${SUPABASE_DB_USERNAME:postgres.odftsymuunuigqgvjsqb}
    password: ${SUPABASE_DB_PASSWORD}
    hikari:
      maximum-pool-size: ${SUPABASE_DB_MAX_POOL_SIZE:5}
      minimum-idle: ${SUPABASE_DB_MIN_IDLE:0}
      idle-timeout: ${SUPABASE_DB_IDLE_TIMEOUT_MS:10000}
      max-lifetime: ${SUPABASE_DB_MAX_LIFETIME_MS:600000}
      connection-timeout: ${SUPABASE_DB_CONNECTION_TIMEOUT_MS:10000}
